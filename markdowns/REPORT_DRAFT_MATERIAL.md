# SQPP Report Draft Material (Code-Grounded, Non-Final)

This draft material is for rewriting in your own voice before submission.

## Part 1: Five Updated Paragraphs

The current system framing remains a binary classification task where each SQL query is assigned to either a fast or slow runtime tier based on measured execution behavior, and this framing is still anchored to BIRD Mini-Dev rather than the earlier TPC-H-only direction. The codebase evidence confirms that the root pipeline (`main.py` plus `src/data/load_bird.py`, `src/features/extract_features.py`, `src/models/train.py`, and `src/evaluation/evaluate.py`) is still organized around this classifier objective, with runtime timing generated from executable SQL and labels produced from timing distributions. The project has also matured from a single-script baseline into a two-track architecture, where System A is the established classifier workflow and System B (`sql_runtime_predictor/`) represents a regression-oriented extension path. The progress document says this System B direction is intentionally presented as designed or partially implemented, and that is consistent with repository state where modules and structure exist, while end-to-end final artifacts and final headline metrics for that subsystem are not presented in the same way as the classifier pipeline. This distinction matters for marking because it shows correct scope control and evidence discipline. [UNDOCUMENTED CHANGE] The codebase audit additionally shows a production-style Streamlit interface with multi-page navigation, integrated artifact loading, and live measured versus predicted comparison logic, which is a stronger system framing than a notebook-only prototype and was not fully emphasized in the progress status. The framing should therefore state that the project is no longer just model experimentation, but an integrated predictive pipeline plus interactive analysis surface. Any claim that this constitutes a complete dual-system deployment must remain qualified because audit notes flag one documentation inconsistency in `streamlit_app/README.md` about runtime-regression wiring, so the report should explicitly separate confirmed implemented behavior from narrative claims.

The data and feature pipeline currently implemented is materially richer than the prior checkpoint and is fully traceable to named files. Data volume increased from 425 raw and 320 labelled rows to 498 raw and 374 labelled rows (`data/query_dataset_raw.csv`, `data/query_dataset_features.csv`), and this change is explicitly reported in progress notes and confirmed by the audit snapshot. Queries are loaded and timed through `src/data/load_bird.py`, where the execution flow retains SQLite/MySQL conversion fallback and adds operational diagnostics such as categorized failure modes, per-database success counters, and sampled failure logging. [UNDOCUMENTED CHANGE] These diagnostic counters and error bucketing are present in the code audit but not called out in the progress narrative, and they should be acknowledged because they directly support reproducibility and debugging transparency. Feature generation still uses the same 25 structural SQL features in `config.py` and `src/features/extract_features.py`, including lexical complexity, join and predicate counts, subquery indicators, aggregation signatures, nesting depth, and clause-specific flags (`has_group_by`, `has_union`, `has_exists`, and related fields). No additional root-classifier features were detected, which is important because it isolates recent improvements to data scale, training strategy, and evaluation coverage rather than feature-schema drift. Labeling remains configurable with `LABEL_METHOD`, with quantile mode as default and middle-band dropping behavior retained; therefore any report text should avoid claiming a new labeling paradigm unless further evidence is added. The abandoned TPC-H-only approach should be described as an initial data-generation path that was replaced by BIRD Mini-Dev grounded timing data, not as a co-equal final dataset source. If claiming synthetic augmentation beyond this, that is currently [NOT FOUND IN CODE], so the writeup should not imply synthetic generation unless new audited scripts or outputs are provided.

Since the last checkpoint, the most meaningful change is not a dramatic uplift in the baseline test score, but a broadening of the experimentation and evidence layer around that baseline. The baseline best model remains XGBoost with cross-validation F1 of 0.5359 plus or minus 0.0406, while held-out test performance remains weak with F1 0.1860, ROC-AUC 0.4610, and accuracy 0.5205. This should be presented candidly as a generalization challenge under schema-aware holdout conditions, not as a finished high-performing predictor. The progress notes and audit both support this interpretation through per-database and per-difficulty splits, including simple queries at F1 0.0000 and only modest scores for challenging and moderate groups. The iterative improvement cycle then introduces quick experiments through `rerun_quick_improvements.py` and corresponding summaries in `reports/quick_experiments_summary.csv` and `.md`. Experiment 1 (weighted logistic regression with `class_weight=balanced`, threshold 0.50) improves target-class metrics over baseline in this run, with F1_slow 0.2632 versus 0.1860 baseline, precision_slow 0.1613 versus 0.1111, recall_slow 0.7143 versus 0.5714, and ROC-AUC 0.6602 versus 0.4610. Experiment 2 (same weighted logistic, threshold 0.38) increases recall but drops F1_slow to 0.1587, so it is retained as a negative result that informs threshold strategy. [NOT FOUND IN CODE] Exact previous-checkpoint model metrics prior to this baseline are not provided in either input document, so claims of numerical improvement relative to an older baseline should be tagged or omitted unless you add source-backed values from earlier reports. [UNDOCUMENTED CHANGE] The audit also shows that all trained models are now persisted in artifacts, which strengthens reproducibility and model comparison reliability beyond what a best-model-only workflow can offer.

The remaining work should now be framed as consolidation and evidence hygiene rather than major algorithmic redesign, because the outstanding section in `PROGRESS_STATUS.md` emphasizes deliverable completion and claim control. The first required task is finalizing the 2000-word Task 1 report using one internally consistent metric snapshot, which means selecting the audited baseline and quick-experiment figures and avoiding mixed historical numbers. The second is to insert and discuss a clean Baseline versus Exp1 versus Exp2 comparison table with rationale for retaining Exp1 as the preferred iterative variant for target-class emphasis. This is directly aligned with the recorded decision logic that class weighting produced a better precision-recall tradeoff for the slow class, while threshold tuning in Exp2 did not improve F1 in the same run. The third outstanding task is operational packaging for assessment, namely a Streamlit Community Cloud URL plus reproducible run and deployment commands included in submission materials. The fourth is documentation consistency cleanup, particularly reconciling any README statements that conflict with observed code behavior, including the known Streamlit runtime-prediction messaging mismatch flagged in the audit. The fifth is a controlled claim on System B: if you assert full runtime-regression outcomes, you must run end-to-end and publish artifacts and metrics; otherwise keep it explicitly as future direction. [NOT FOUND IN CODE] Any outstanding task outside this list, such as introducing a new feature family, changing split strategy to LODOCV, or adding unplanned deep architecture changes, is not supported by the provided progress status and should not be inserted into this section. This discipline strengthens marking alignment under communication and scientific reporting criteria.

The evaluation framework currently combines database-aware holdout testing with cross-validation during training, and this hybrid setup should be described as a deliberate integrity choice rather than an inconsistency. The training module keeps a four-model registry, Logistic Regression, Random Forest, Gradient Boosting, and XGBoost, and uses 5-fold stratified CV with expanded metric capture beyond F1, including accuracy, precision, recall, and ROC-AUC via `cross_validate`. Selection evidence remains strongest for XGBoost on CV mean F1 at 0.5359 compared with Random Forest 0.5106, Gradient Boosting 0.4741, and Logistic Regression 0.2694. However, held-out outcomes indicate the selected model does not transfer strongly to the reserved databases, so model selection should be reported as best within current constraints, not universally robust. The evaluation layer has materially expanded and now generates all-model test comparison, classification report CSV, per-database and per-difficulty breakdowns, confusion matrices (raw and normalized), ROC and precision-recall curves, calibration curve, CV fold table and boxplot, class and runtime distributions, split summary, error analysis, and optional learning-curve flow. [UNDOCUMENTED CHANGE] The audit also notes significance testing logic (Wilcoxon, when SciPy is present), which should be mentioned as an emerging statistical-comparison mechanism if you confirmed dependencies during execution. [NOT FOUND IN CODE] There is no evidence in the provided documents that LODOCV replaced the current strategy; therefore report text should not claim a switch to leave-one-database-out CV unless new audited code proves it. The current best-model statement is therefore, XGBoost remains baseline winner by CV ranking, while Exp1 weighted logistic is retained as a task-focused alternative with improved slow-class metrics in quick iteration.

## Part 2: Development Workflow Graph (SVG)

The SVG is stored at `markdowns/development_workflow.svg`.

